name: 'stride_accountability'
version: '1.0.0'
config-version: 2

profile: 'stride_accountability'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  stride_accountability:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
